# E1 主表 `ads_l8_unified_signal` 增量改造

**Epic 描述**: 在不动现有字段的前提下，增加 4 个字段为后续评估、扩展、视图切换打基础。预计耗时 2 个工作日。

---

## E1-S1 主表加 4 字段

**作为** L8 评分流程, **我希望** 主表能记录评分版本、异动机制分类、评分溯源和推送状态, **以便** 评估系统能按版本回测、按分类分组、按推送状态过滤。

#### 任务
- [x] E1-S1-T1 编写 `ALTER TABLE` 迁移脚本 (MySQL)
- [x] E1-S1-T2 编写 ClickHouse 同步表的对应字段添加脚本
- [x] E1-S1-T3 跨仓 schema 变更四件套 (变更说明 / 影响清单 / 迁移脚本 / rollback)
- [x] E1-S1-T4 在 `IMPLEMENTATION_FEEDBACK.md` 标注跨仓变更

#### 字段定义
... (省略)

#### 验收标准 (AC)
- [x] **AC1: 字段添加完成且无业务中断**
- [x] **AC2: 索引可用**
- [x] **AC3: 双写一致**

---

## E1-S2 激活 `dim_anomaly_score_weight` 版本化使用

#### 任务
- [x] E1-S2-T1 在 `dim_anomaly_score_weight` 写入 v1 配置 (4 个权重 + meta)
- [x] E1-S2-T2 评分代码读取 v1 权重配置, 而不是硬编码

#### 验收标准 (AC)
- [x] **AC1: 评分代码读配置不读硬编码**

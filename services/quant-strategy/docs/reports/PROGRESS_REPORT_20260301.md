# 另类数据开发与实盘化迁移工作报告 (2026-03-01)

## 核心交付内容 (Core Deliverables)
本阶段完成了 **EPIC-018: 硬件算力数据穿透** 的实盘化迁移与核心解析算法升级。系统已彻底删除模拟数据，全面接入真实现的场采集。

### 1. 政企招投标数据深度采集 (CCGP Breakthrough)
* **`services/altdata-source/src/collectors/procurement.py`**:
  - **详情页二次穿透**：实现了先抓取列表、再异步深入详情页的二级爬虫架构。
  - **金额提取算法**：采用正则 + 专用 CSS 标签（`.bookmark-item`）的双重策略提取中标金额，并实现了“元”到“万元”的归一化换算。
  - **中标人清洗**：优化了 winner 字段的提取逻辑，增加了 9 条干扰词过滤规则，确保中标单位名称的纯净性。
  - **采集礼仪**：引入了 3.0s 的请求延迟间隔，显著降低了触发 CCGP 反爬封锁的风险。

### 2. 硬件实盘化迁移与清理 (Migration & Cleanup)
* **模拟数据清除**：
  - 移除了 `ProcurementCollector` 和 `HardwareSpotCollector` 中所有 Hardcoded 的 Mock 样本。
  - 执行 `TRUNCATE` 清空了 ClickHouse 中的测试表，确保后续回测与信号观测的数据纯净。
* **现货价格接入**：
  - **AutoDL**：切换至官方 API 采集链路，移除内部硬编码，支持通过 `.env` 注入 Token 进行授权访问。

### 3. 代码质量优化与加固 (Code Quality Audit)
通过对 `altdata-source` 服务进行深度审计，修复了 **6 项** 关键质量隐患：
* **时区漂移修复**：将 `self.now` 构造快照改为实时计算的 `_now` 属性，解决长期运行后的采集时间戳滞后问题。
* **异常处理加固**：清理了裸 `except: pass`，替换为有类型的异常捕获。
* **代码除重**：删除了 `_parse_tender_detail` 中的重复金额解析死代码。

### 4. 选股信号联动增强 (Strategy Integration)
* **`services/quant-strategy/src/config/altdata_mapping.py`**:
  - 新增了 `ComputeCenter` (智算/超算/服务器) 硬件类型。
  - 映射至“智算中心”、“算力租赁”等 A 股核心概念，实现了基建类投资热度对股价红利的传导。

## 已完成验证
- [x] **数据落库验证**：成功采集到 2026 年 2 月真实的 7 条服务器中标记录，金额分别为 1.3 - 248 万元不等。
- [x] **导入冒烟测试**：跨模块依赖关系通过 `python3 -c` 静态校验。

## 后续建议
1. **反爬增强**：若需进一步提高采集频率，建议在 `collectors` 层引入分布式代理池。
2. **算力广度**：建议后续接入“沐曦”等国产芯片的海外贸易与禁令相关另类指标。
